# Variables de entorno requeridas para el Group Service.
# Define la conexión a la base de datos MariaDB compartida.

# ---- Conexión a MariaDB ----
DB_HOST=mariadb
DB_USER=wallet_user
DB_PASS=walletpass
DB_NAME=wallet_db